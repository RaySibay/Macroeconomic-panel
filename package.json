{
  "name": "china-free-liquidity-panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev:local": "node scripts/local_dev.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:schema": "wrangler d1 execute china-liquidity --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
